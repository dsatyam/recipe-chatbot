# Pin runtime dependencies (install with: pip install -r requirements.txt).
# Same libraries as pyproject.toml [project] dependencies — see PROJECT_WALKTHROUGH.md.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
python-dotenv>=1.0.1
pydantic-settings>=2.6.0
openai>=1.57.0
